%% 
% fc = [100;100;10;-pi/8] 
function fc = prep_fc_for_sift(R)

    [x_grid ,y_grid] = meshgrid(1:size(R,2),1:size(R,1));

    fc = [x_grid(transA_Rsup>0) y_grid(transA_Rsup>0)];
    fc = [fc ones(size(fc,1),1)*10];
    fc = [fc transA_grad_dir(transA_Rsup>0)];
    fc = fc';


end